name: Run primary generation speed optimization

on:
  push:
    branches:
      - claude/image-morphing-video-3kujgb
    paths:
      - .github/workflows/cf-run-generation-hotfix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: claude/image-morphing-video-3kujgb
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply and validate primary-generation speed patch
        run: python3 scripts/cf_generation_hotfix.py
      - name: Commit result
        run: |
          git config user.name "caseflow-performance"
          git config user.email "actions@users.noreply.github.com"
          git add caseflow_studio_v96.html docs/generation-hotfix-result.txt
          git commit -m "perf(simulation): target sub-30-second primary generation" || exit 0
          git push origin HEAD:claude/image-morphing-video-3kujgb
