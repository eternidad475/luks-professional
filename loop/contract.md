## acts alone
draft PRs on branches; fix lint/syntax debt; update loop/memory/STATE.md;
label issues; docs/ typo fixes

## queues for me
api/stripe/, supabase/migrations/, service workers (sw.js and app*/service-worker.js),
vercel.json, auth flows; any skill below "auto" tier; any diff > 400 lines

## wakes me up
verify fails twice on the same item
safeguard router swapped models mid-run
daily budget breached
anything requests a secret (Stripe keys, Supabase service role, Modal tokens)
a standing goal is VIOLATED
